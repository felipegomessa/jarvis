# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# venv (uv)
.venv/
venv/
env/

# Secrets
.env
*.env
!.env.example

# SQLite (índice regenerável via `populate` + `seed_demo`)
*.db
*.db-journal
*.sqlite
*.sqlite3

# Dataset: livros-texto de terceiros (~88 MB) — recuperáveis pelo link do Drive
# documentado em data/README.md. Os 5 docs indexados (Artigos + Material de Aula)
# permanecem versionados.
data/Livros/

# Logs
logs/
*.log

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# Models cache (sentence-transformers)
.cache/
models/

# Jupyter
.ipynb_checkpoints/

# Build
build/
dist/

# Arquivos temporários de IDE/editor (snapshots de sessão)
*.tmp.*
# Lock files do Microsoft Word
~$*.docx
~$*.doc
~WRL*.tmp

# Documentação de entrega gerada (Word/PDF) — regerável via scripts/
docs/

# Roteiro do vídeo (uso pessoal, não vai pro repo)
ROTEIRO.md

# Caches/efêmeros internos (não versionar)
.claude/
temp/
memory/

# Os docs de continuidade e do processo SDD SÃO versionados intencionalmente
# (CLAUDE.md, decisions.md, STATUS.md, NOTES.md, HANDOFF.md, spec/,
# PLANO_MELHORIAS_T1.md) — permitem retomar o projeto de outra sessão.
